Module Module1

    ''' <summary>
    ''' Point d'entrée du traitement par lot. Le corps du traitement reste à
    ''' écrire : ce module affichait une boîte de dialogue de mise au point, ce qui
    ''' bloquait indéfiniment une exécution planifiée sans session interactive.
    ''' </summary>
    Sub Main()
        Console.WriteLine("Aucun traitement par lot n'est implémenté.")
        Environment.ExitCode = 0
    End Sub

End Module
